export default class Message {
  constructor({ id, username, firstName, lastName, message,}) {
    this.id = id
    this.username = username
    this.firstName = firstName
    this.lastName = lastName
    this.message = message
  }
}
